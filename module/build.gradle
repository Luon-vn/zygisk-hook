plugins {
    id 'com.android.library'
}

android {
    compileSdkVersion 32
    // ndkVersion '25.2.9519653'

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 32
    }

    externalNativeBuild {
        ndkBuild {
            path("jni/Android.mk")
        }
    }
}
